  <div class="contents" >
    <% @posts.each do |post| %>
      <div class="content_post">
            <div class="title"><%= post.text.length %></div>

            <div class="index-title">
            <%= post.title %>
            <%= link_to '記事をみる', "/posts/#{post.id}", method: :get, class: "link-text" %>
            </div>
      </div>
    <% end %>
  </div>